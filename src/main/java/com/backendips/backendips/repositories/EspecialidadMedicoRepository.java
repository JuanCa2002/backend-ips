package com.backendips.backendips.repositories;

import com.backendips.backendips.models.EspecialidadMedico;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EspecialidadMedicoRepository extends JpaRepository<EspecialidadMedico, Integer> {
}
